# 太空建造游戏 - 建造UI系统实现文档

## 1. 系统概述

本系统实现了一个完整的游戏内建造UI，包含建筑分类浏览、图标选择、滚轮翻页、建造模式和资源校验功能。

## 2. 文件结构

```
Assets/
├── Scripts/
│   ├── GameCore/
│   │   ├── Enums.cs              # 枚举定义（建筑分类）
│   │   ├── BuildingData.cs       # 建筑数据结构
│   │   └── DataConfig.cs         # 数据配置
│   ├── GridSystem/
│   │   └── BuildingPlacer.cs     # 建筑放置器（已更新）
│   └── UI/
│       ├── BuildingUI.cs         # 主UI管理器
│       ├── BuildingCategoryPanel.cs  # 分类面板
│       └── BuildingIconButton.cs     # 建筑图标按钮
└── Resources/
    └── Icons/
        └── Buildings/            # 建筑图标存放位置
```

## 3. 核心功能

### 3.1 建筑分类系统

建筑分为6个分类：

| 分类         | 说明   | 包含建筑                   |
| ---------- | ---- | ---------------------- |
| Core       | 核心设施 | 太空紧急避难仓                |
| Power      | 能源设施 | 核能发电模块、太阳能发电阵列         |
| Production | 生产设施 | 采矿平台、熔炉精炼厂、零件组装厂、高级加工厂 |
| Logistics  | 物流设施 | 传送带、分拣器                |
| Storage    | 仓储设施 | 太空仓储对接舱                |
| Special    | 特殊设施 | 飞船组装平台                 |

### 3.2 UI组件说明

#### BuildingUI (主管理器)

- 位置：`Assets/Scripts/UI/BuildingUI.cs`
- 功能：
  - 管理所有分类面板
  - 处理全局输入（B键切换UI显示）
  - 管理建造模式
  - 显示建筑信息提示框
  - 连接BuildingPlacer和GameManager

#### BuildingCategoryPanel (分类面板)

- 位置：`Assets/Scripts/UI/BuildingCategoryPanel.cs`
- 功能：
  - 显示分类名称和图标
  - 生成该分类下的所有建筑图标
  - 支持展开/折叠
  - 滚轮翻页浏览
  - 自适应排列图标

#### BuildingIconButton (建筑图标按钮)

- 位置：`Assets/Scripts/UI/BuildingIconButton.cs`
- 功能：
  - 显示建筑图标和名称
  - 资源不足时显示锁定状态
  - 点击事件处理
  - 鼠标悬停显示提示

## 4. Unity设置指南

### 4.1 创建建筑UI Canvas

1. 在场景中创建新的Canvas
2. 设置Render Mode为 **Screen Space - Overlay**
3. 创建以下UI结构：

```
Canvas
├── BuildingUI (空物体，挂载BuildingUI脚本)
│   ├── MainPanel (Panel，主面板)
│   │   ├── CategoryContainer (Vertical Layout Group)
│   │   │   ├── CategoryPanel_Core (BuildingCategoryPanel)
│   │   │   ├── CategoryPanel_Power (BuildingCategoryPanel)
│   │   │   ├── CategoryPanel_Production (BuildingCategoryPanel)
│   │   │   ├── CategoryPanel_Logistics (BuildingCategoryPanel)
│   │   │   ├── CategoryPanel_Storage (BuildingCategoryPanel)
│   │   │   └── CategoryPanel_Special (BuildingCategoryPanel)
│   └── TooltipPanel (Panel，信息提示框)
└── BuildingPlacer (空物体，挂载BuildingPlacer脚本)
```

### 4.2 BuildingCategoryPanel设置

每个分类面板需要配置：

- **category**：选择对应的BuildingCategory
- **categoryButton**：展开/折叠按钮
- **categoryNameText**：分类名称文本
- **contentContainer**：图标容器
- **buildingIconPrefab**：建筑图标预制体
- **scrollView**：Scroll View对象
- **scrollRect**：ScrollRect组件
- **iconsPerRow**：每行图标数（推荐4）
- **iconSpacing**：图标间距
- **iconSize**：图标大小

### 4.3 BuildingIconButton预制体设置

1. 创建Button作为预制体
2. 添加BuildingIconButton脚本
3. 配置引用：
   - **iconImage**：图标Image组件
   - **buildingNameText**：建筑名称Text
   - **selectedIndicator**：选中指示器（如高亮边框）
   - **lockedOverlay**：锁定遮罩

### 4.4 连接脚本引用

在BuildingUI inspector中：

- 将所有BuildingCategoryPanel拖入categoryPanels数组
- 连接tooltipPanel及其组件
- 连接buildingPlacer和mainCamera
- 设置toggleKey（默认B键）

### 4.5 建筑图标准备

1. 创建文件夹：`Assets/Resources/Icons/Buildings/`
2. 为每个建筑创建Sprite资源
3. 图标路径示例：
   - `Assets/Resources/Icons/Buildings/EmergencyShelter.png`
   - `Assets/Resources/Icons/Buildings/MiningPlatform.png`

## 5. 使用流程

### 5.1 打开建造UI

- 按**B键**切换建造UI的显示/隐藏

### 5.2 浏览建筑

1. 点击分类按钮展开/折叠分类
2. 使用**鼠标滚轮**在分类内翻页
3. 鼠标悬停建筑图标查看详细信息

### 5.3 建造建筑

1. 点击可建造的建筑图标（资源充足时可用）
2. 进入建造模式，鼠标跟随显示预览
3. 绿色预览表示可放置，红色表示不可放置
4. **左键点击**放置建筑
5. **右键或ESC**取消建造模式

### 5.4 拆除建筑

- 在非建造模式下，**右键点击**已放置的建筑可拆除

## 6. 数据配置

### 6.1 添加新建筑

在`DataConfig.cs`的`InitializeBuildings()`方法中添加：

```csharp
var newBuilding = new BuildingDefinition(
    BuildingType.NewBuildingType,    // 建筑类型
    BuildingCategory.Production,     // 分类
    "新建筑名称",                    // 名称
    "建筑描述",                      // 描述
    width: 2,                        // 宽度
    height: 2,                       // 高度
    powerConsumption: 10,            // 耗电
    powerProduction: 0,              // 发电
    storageCapacity: 0,              // 存储
    isProductionBuilding: false,     // 是否生产建筑
    prefabPath: "",                  // 预制体路径
    iconPath: "Icons/Buildings/NewBuilding"  // 图标路径
);
newBuilding.costs.Add(new BuildingCost(ResourceType.SpaceOre, 50));
BuildingDefinitions[BuildingType.NewBuildingType] = newBuilding;
```

### 6.2 添加新分类

1. 在`Enums.cs`中添加新的`BuildingCategory`枚举值
2. 在`DataConfig.cs`的`GetCategoryName()`方法中添加对应中文名称
3. 在Unity中创建新的BuildingCategoryPanel

## 7. 扩展功能建议

### 7.1 建筑旋转

- 添加R键旋转功能
- 修改BuildingPlacer支持旋转预览

### 7.2 快捷建造

- 添加数字快捷键选择建筑
- 保存常用建筑快捷栏

### 7.3 批量建造

- 支持按住Shift连续放置
- 拖动选择区域批量放置

### 7.4 撤销/重做

- 记录建造操作历史
- 添加Ctrl+Z撤销功能

### 7.5 建筑搜索

- 添加搜索框
- 按名称筛选建筑

## 8. 注意事项

1. 所有Resources文件夹下的资源路径区分大小写
2. 确保GameManager和GridManager在场景中存在
3. 建筑预览预制体应包含Renderer组件用于颜色反馈
4. 建议使用异步加载处理大量图标资源
5. 资源不足时，建筑按钮会显示灰色锁定状态

## 9. 脚本API参考

### BuildingUI 公共方法

```csharp
public static BuildingUI Instance { get; }
public void ToggleUI()
public void ShowUI()
public void HideUI()
public void SelectBuilding(BuildingIconButton button)
public void ShowBuildingTooltip(BuildingDefinition def, Vector3 screenPosition)
public void HideBuildingTooltip()
public void UpdateAllAffordability()
```

### BuildingCategoryPanel 公共方法

```csharp
public void InitializeCategory()
public void ToggleExpand()
public void SetExpanded(bool expanded)
public void HandleScroll(float scrollDelta)
public void UpdateAffordability()
public void DeselectAll()
```

### BuildingIconButton 公共方法

```csharp
public void Initialize(BuildingDefinition def, bool canAfford)
public void UpdateAffordability()
public void SetSelected(bool selected)
public event OnBuildingSelected OnSelected;
```

